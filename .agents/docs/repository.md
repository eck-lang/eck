# Repository conventions

## Language

All documentation, code comments, and commit messages must be written in English.

This includes Markdown files under `.agents/docs/`, Rust doc comments (`///`, `//!`), inline code comments (`//`), and Conventional Commit subjects, bodies, and footers. Do not use other languages for these artifacts, even when the conversation with the user happens in another language.
