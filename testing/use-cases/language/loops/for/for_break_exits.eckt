# for range loop exits through break

Verifies `break` also terminates the enclosing `for` range loop immediately,
leaving the remaining range unvisited.

>>> source
for (i in 0..10) {
    if (i == 3) {
        break
    }

    print(i)
}

<<< stdout
0
1
2

<<< stderr
