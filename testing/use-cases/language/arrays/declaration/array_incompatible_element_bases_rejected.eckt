# Dynamic array elements keep unrelated concrete types

Verifies an unannotated array literal accepts unrelated element types without
coercing or erasing their concrete identities.

>>> source
let values = [10mm, "hello"]
print(values)

<<< stdout
[10mm, hello]

<<< stderr
