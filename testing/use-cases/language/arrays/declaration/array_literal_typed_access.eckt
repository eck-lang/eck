# Typed array literal element access

Verifies an `int[]` binding initialized from a literal reads its elements by
zero-based index in source order.

>>> source
let numbers: int[] = [1, 2, 3]
print(numbers[0])
print(numbers[2])

<<< stdout
1
3

<<< stderr
