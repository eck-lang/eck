# Unannotated empty array is dynamic

Verifies an empty unannotated array accepts values of unrelated concrete types.

>>> source
let values = []
values->push(10)
values->push("hello")
print(values)

<<< stdout
[10, hello]

<<< stderr
