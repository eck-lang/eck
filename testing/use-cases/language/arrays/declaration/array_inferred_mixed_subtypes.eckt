# Dynamic array keeps each element subtype

Verifies an unannotated array literal keeps every measure element's concrete
subtype without creating an element contract.

>>> source
let sizes = [10mm, 2cm, 3dm]
print(sizes[0])
print(sizes[1])
print(sizes[2])

<<< stdout
10mm
2cm
3dm

<<< stderr
