# Declared and dynamic string arrays

Verifies string elements work with an explicit `string[]` contract and a
dynamic unannotated array, while both preserve source order during reads.

>>> source
let typed: string[] = ["alice", "bob"]
let inferred = ["carol", "dave"]
print(typed[1])
print(inferred[0])

<<< stdout
bob
carol

<<< stderr
