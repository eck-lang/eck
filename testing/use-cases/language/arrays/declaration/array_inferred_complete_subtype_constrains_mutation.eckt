# Dynamic arrays preserve inserted subtype identity

Verifies an unannotated array does not create a subtype contract and therefore
stores a later centimeter value with its own concrete identity.

>>> source
let sizes = [10mm, 20mm]
sizes[1] = 2cm
print(sizes[0])
print(sizes[1])

<<< stdout
10mm
2cm

<<< stderr
