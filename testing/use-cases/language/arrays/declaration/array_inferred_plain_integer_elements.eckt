# Plain integers in a dynamic array

Verifies an unannotated integer literal creates a dynamic array while preserving
source order and exact values during element access.

>>> source
let values = [1, 2, 3]
print(values[0])
print(values[2])

<<< stdout
1
3

<<< stderr
