# Dynamic array stores a promoted element expression

Verifies an unannotated array adds no `int8` element contract, so the promoted
`int16` result keeps its concrete value instead of being narrowed or rejected.

>>> source
let x: int8 = 127
let y: int8 = 1
let a = [x + y]
print(a)

<<< stdout
[128]

<<< stderr
