# Dynamic array end and indexed mutation cycle

Measures repeated push, pop, unshift, shift, and indexed replacement on a
dynamic array while keeping its live size bounded.

>>> checkpoint array-benchmark-introduction 73b9eaca434ae8064ec528bf039df87bf5d57dfd
Starting point: this commit introduced the benchmark runner and the first
array workloads, giving later array changes a historical comparison.

>>> source
let values = [0]
for (iteration in 0..100000) {
    values->push(iteration)
    values->pop()
    values->unshift(iteration)
    values->shift()
    values[0] = iteration
}
print(values)
